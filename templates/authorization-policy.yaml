{{- if .Values.istio.enabled }}
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: {{ include "identity-service.fullname" . }}-allow-internal
  labels:
    {{- include "identity-service.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "identity-service.selectorLabels" . | nindent 6 }}
  action: ALLOW
  rules:
  # Allow internal service-to-service calls to /api/v1/internal/* endpoints
  - from:
    - source:
        principals: {{ .Values.istio.authorization.internalCallers | toJson }}
    to:
    - operation:
        methods: ["GET", "POST", "PUT", "DELETE"]
        paths: ["/api/v1/internal/*"]

  # Allow public endpoints (auth, docs, health) from anyone
  - to:
    - operation:
        methods: ["GET"]  
        paths:
          - "/v3/api-docs/*"
          - "/v3/api-docs"
          - "/swagger-ui/*"
          - "/swagger-ui.html"
          - "/actuator/*"
          - "/actuator/health"
          - "/actuator/health/*"
    - operation:
        methods: ["GET", "POST", "PUT", "DELETE"]
        paths:
          - "/api/v1/auth/*"
          - "/api/v1/admin/*"
          - "/api/v1/reference/*"
          - "/api/v1/legal/*"
{{- end }}
